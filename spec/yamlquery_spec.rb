require "spec_helper"

describe Yamlquery do
  it "has a version number" do
    expect(Yamlquery::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
